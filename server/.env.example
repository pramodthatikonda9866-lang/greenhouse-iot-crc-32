# Port for Express API server
PORT=5000

# Optional MongoDB connection URI (e.g. mongodb://localhost:27017/greenhouse_iot)
# If omitted, the server automatically falls back to an embedded MongoMemoryServer
MONGODB_URI=
